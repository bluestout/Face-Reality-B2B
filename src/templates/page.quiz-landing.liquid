{% comment %} TODO background image & links {% endcomment %}
{% comment %} TODO remove the placeholder when done with development {% endcomment %}
<div class="quiz-landing">
  <div class="quiz-landing__banner">
    <img src="https://via.placeholder.com/160x105" alt="#" class="quiz-landing__image"/>
    <h1 class="quiz-landing__title">{{ page.title }}</h1>
    <div class="quiz-landing__text">{{ page.content }}</div>

    {% section 'certification-landing' %}

  </div>
</div>
