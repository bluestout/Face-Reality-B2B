{% layout none %}
{% assign downloads = blogs.downloads %}
{
  "assets": [
    {% paginate downloads.articles by 1000 %}
      {% for item in downloads.articles %}
        {{ item | json }}
        {% unless forloop.last %},{% endunless %}
      {% endfor %}
	  {% endpaginate %}
  ]
}