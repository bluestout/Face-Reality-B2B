<div class="responsive-table">
  <table>
    <thead>
      <tr>
        <th>{{ section.settings.method_head }}</th>
        <th>{{ section.settings.rate_head }}</th>
        <th>{{ section.settings.time_head }}</th>
        <th>{{ section.settings.carrier_head }}</th>
      </tr>
    </thead>
    <tbody>
      {% for block in section.blocks %}
        <tr>
          <td>{{ block.settings.method }}</td>
          <td>{{ block.settings.rate }}</td>
          <td>{{ block.settings.time }}</td>
          <td>{{ block.settings.carrier }}</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
</div>

{% schema %}
  {
    "name": "Shipping table",
    "settings": [
      {
        "type": "text",
        "id": "method_head",
        "label": "Method header"
      },
      {
        "type": "text",
        "id": "rate_head",
        "label": "Rate header"
      },
      {
        "type": "text",
        "id": "time_head",
        "label": "Time header"
      },
      {
        "type": "text",
        "id": "carrier_head",
        "label": "Carrier header"
      }
    ],
    "blocks": [
      {
        "type": "block",
        "name": "Type",
        "settings": [
          {
            "type": "text",
            "id": "method",
            "label": "Method"
          },
          {
            "type": "text",
            "id": "rate",
            "label": "Rate"
          },
          {
            "type": "text",
            "id": "time",
            "label": "Delivery time"
          },
          {
            "type": "text",
            "id": "carrier",
            "label": "Carrier"
          }
        ]
      }
    ]
  }
{% endschema %}
