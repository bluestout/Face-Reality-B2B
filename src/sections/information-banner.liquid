{% assign light_font = false %}
{% if section.settings.color == "white" %}
  {% assign light_font = true %}
{% endif %}

{% assign b_title = page.content | strip | strip_html %}
{% include 'page-banner' with
  background: section.settings.bg,
  background_mobile: section.settings.bg_mobile,
  banner_suptitle: page.title,
  banner_title: section.settings.subtitle
  light_font: light_font
  height: "medium"
  margin: "none",
  limit_width: "large"
%}

{% schema %}
  {
    "name": "Banner",
    "settings": [
      {
        "type": "image_picker",
        "id": "bg",
        "label": "Background"
      },
      {
        "type": "image_picker",
        "id": "bg_mobile",
        "label": "Mobile Background"
      },
      {
        "type": "text",
        "id": "subtitle",
        "label": "Subtitle"
      },
      {
        "type": "radio",
        "id": "color",
        "label": "Font Color",
        "options": [
          { "value": "black", "label": "Black" },
          { "value": "white", "label": "White" }
        ]
      }
    ]
  }
{% endschema %}