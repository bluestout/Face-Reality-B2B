{% if section.settings.bg %}
  <div class="testimonials-banner" data-bgset="{% include 'responsive-bg-image', image: section.settings.bg %}">
{% else %}
  <div class="testimonials-banner">
{% endif %}
  <h1 class="testimonials-banner__suptitle">{{ page.title }}</h1>
  <h2 class="testimonials-banner__title">{{ page.content | strip | strip_html }}</h2>
</div>

{% schema %}
  {
    "name": "Header Banner",
    "settings": [
      {
        "type": "image_picker",
        "id": "bg",
        "label": "Background"
      }
    ]
  }
{% endschema %}
