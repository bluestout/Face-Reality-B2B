<nav role="navigation" class="primary-nav">
  <ul class="primary-nav__list">
    {% for link in linklists[section.settings.menu].links %}
      {% assign handle = link.title | handleize %}
      {% if handle == "our-products" %}
        {% if customer %}
          <li class="primary-nav__item">
            <a href="{{ link.url }}" class="primary-nav__link" data-primary-nav-link>
              {{ link.title }}
              <span class="icon-angle-down primary-nav__icon"></span>
            </a>
            <ul class="primary-nav__sub-list" data-primary-nav-sublist>
              {% for childlink in link.links %}
                <li class="primary-nav__sub-item">
                  <a href="{{ childlink.url }}" class="primary-nav__sub-link">{{ childlink.title }}</a>
                </li>
              {% endfor %}
            </ul>
          </li>
        {% else %}
          <li class="primary-nav__item">
            <a href="/pages/about-our-products" class="primary-nav__link">{{ link.title }}</a>
          </li>
        {% endif %}
      {% else %}
        {% if link.links != blank %}
          <li class="primary-nav__item">
            <a href="{{ link.url }}" class="primary-nav__link" data-primary-nav-link>
              {{ link.title }}
              <span class="icon-angle-down primary-nav__icon"></span>
            </a>
            <ul class="primary-nav__sub-list" data-primary-nav-sublist>
              {% for childlink in link.links %}
                <li class="primary-nav__sub-item">
                  <a href="{{ childlink.url }}" class="primary-nav__sub-link">{{ childlink.title }}</a>
                </li>
              {% endfor %}
            </ul>
          </li>
        {% else %}
          {% if link.handle == "training" and customer and customer.tags contains "training_approved" %}
            <li class="primary-nav__item">
              <a href="https://certification.facerealityskincare.com?shopify_id={{ customer.id }}&shopify_email={{ customer.email }}" class="primary-nav__link">{{ link.title }}</a>
            </li>
          {% else %}
            <li class="primary-nav__item">
              <a href="/pages/about-our-products" class="primary-nav__link">{{ link.title }}</a>
            </li>
          {% endif %}
        {% endif %}
      {% endif %}
    {% endfor %}
  </ul>
</nav>

<button type="button" class="primary-nav__menu-button" data-mobile-menu-toggle>
  <span class="primary-nav__menu-button-icon icon-menu"></span>
  <span class="primary-nav__menu-button-text">Menu</span>
</button>